# Development — proxied through package.json "proxy" to http://localhost:8080
REACT_APP_API_URL=/api

# Uncomment for production (replace with your actual backend URL):
# REACT_APP_API_URL=https://your-backend.com/api
